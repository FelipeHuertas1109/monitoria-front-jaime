// Configuración del entorno
// Puedes cambiar la URL aquí directamente o usar variables de entorno
export const environment = {
  backendUrl: process.env.NEXT_PUBLIC_BACKEND_URL || 'http://127.0.0.1:8000',
};
